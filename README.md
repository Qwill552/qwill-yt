# Qwill YT — очередь просмотра YouTube

Личный сайт-очередь для видео с YouTube: вставляешь ссылку — появляется карточка
с названием, каналом, длительностью и датой публикации, отсортированная по
приоритету (Важно / Средне / Позже). Развёрнут на `qwill-yt.mooo.com`.

Так же принимаются ссылки на аниме с `animego.me`: карточка собирается с
постером (он крупнее превью YouTube, поэтому показывается в вертикальном
формате 5:7), названием, студией-аниматором вместо канала, количеством серий
вместо длительности и датой выхода. Такие карточки всегда попадают в
раздел «Аниме».

Счётчик серий на аниме-карточке показывает «просмотрено / доступно»
(`0/12 эп.`): вторым числом всегда идёт количество уже вышедших серий — для
онгоингов берётся именно оно, а не запланированное общее число со знаком `?`.
Клик по счётчику превращает его в поле ввода: вписал цифру — сохранилось.

Собран на TanStack Start (React 19 + Nitro), очередь хранится в `localStorage`
браузера. Для продакшена Nitro собирается с пресетом `node-server` и
запускается как systemd-сервис на порту 4100 за Caddy-реверс-прокси.

## Приём ссылок извне: `/api/queue-ingest`

Поскольку очередь живёт в `localStorage`, а не в базе, для добавления видео
из внешних источников (например, прямо с YouTube) есть небольшой
«почтовый ящик» на сервере:

- `POST /api/queue-ingest` — принимает `{ url, priority }`, требует заголовок
  `Authorization: Bearer <INGEST_TOKEN>`, резолвит метаданные видео и кладёт
  запись в очередь на диске (`data/pending-queue.json`, не версionируется).
  Второй вариант тела — уже готовая карточка аниме:
  `{ source: "animego", url, priority, title, studio, thumbnail, episodes,
  publishedAt }`. Он нужен потому, что часть тайтлов AnimeGO не отдаёт
  анонимным запросам: в браузере с залогиненной сессией страница
  открывается, а серверу прилетает 404. В этом случае страницу читает
  юзерскрипт прямо в браузере и присылает данные сюда.
- `GET /api/queue-ingest` — сайт сам дёргает этот эндпоинт при каждом
  открытии, забирает накопленные ссылки, добавляет их в очередь и очищает
  ящик.

`INGEST_TOKEN` задаётся переменной окружения на сервере (в systemd-юните),
в репозитории не хранится.

## Автообновление серий и уведомления

Счётчик серий обновляется сам у **всех** аниме-карточек очереди, а не только
у подписанных. Очередь живёт в `localStorage`, поэтому список аниме сайт
присылает серверу сам: целиком при каждом открытии (`POST /api/anime-track`)
и по одной карточке в момент добавления. Сервер заводит только неизвестные
ему строки и ничего не перезаписывает — присланный снимок из браузера заведомо
старее того, что уже начитал чекер. Удаление карточки снимает тайтл с
отслеживания явно (`POST /api/anime-untrack`), а не вычитанием из списка: у
каждого браузера очередь своя, и заход с телефона не должен стирать трекинг
карточек, добавленных с компьютера.

Раз в 3 часа сервер перечитывает страницу каждого отслеживаемого тайтла и
обновляет число серий в карточке. Полностью вышедшие (поле «Статус» на
AnimeGO — «Вышел») больше не перечитываются: одна проверка нужна, чтобы это
узнать, дальше строка выпадает из выборки. Время у каждой карточки своё
(карточки заводятся в разные моменты, от них и идёт отсчёт), так что одним
залпом AnimeGO это не бьёт.

**Уведомления** — отдельная история: их получают только подписанные тайтлы.
Подписка включается колокольчиком в карточке аниме, справа от приоритета
(залит — подписка активна); первая проверка идёт сразу по подписке. Уведомления
двух видов:

- **вышла новая серия** — превью, название аниме, название и номер серии;
- **тайтл из вишлиста стартовал** — «Теперь онгоинг!» вместо номера первой
  серии, бейдж «NEW!» (см. раздел про вишлист ниже);
- **тайтл вышел полностью** — когда статус переключается с онгоинга на «Вышел».
  Бейдж «ФУЛЛ» вместо номера, подпись «Вышло полностью, N серий». Нужна база
  для сравнения, поэтому только что добавленное (в том числе уже завершённое)
  аниме о своём финале не сообщает — сначала запоминается статус, уведомление
  приходит только на смену. Если последняя серия и финал приходят одной
  проверкой, шлётся только «вышло полностью»: итоговое число серий в нём есть.

Всё это копится в колокольчике в левом верхнем углу шапки (уведомления живут
30 дней с момента создания независимо от того, прочитаны они или нет,
непрочитанные отделены чертой) и дублируется всплывающей плашкой внизу экрана.

Плашка показывается и живьём, и «в догонку»: то, что пришло, пока сайт был
закрыт, всплывает при следующем заходе — наибольший показанный id хранится в
`localStorage`, поэтому дважды одно и то же не всплывёт. Несколько уведомлений
схлопываются в одну плашку («Новых серий: 4»). Закрыть её можно **только**
крестиком; клик по самой плашке открывает список колокольчика и закрывает её.
Таймаута и свайпа у неё нет, а таб-бар она не перекрывает.

Часть тайтлов сервер не может перечитать анонимно (та же защита, что не
пускает `/api/queue-ingest` к некоторым карточкам при добавлении — см.
выше). Такие тайтлы дочитывает `qwill-yt.user.js`: пока открыта любая
страница AnimeGO, скрипт спрашивает `GET /api/anime-recheck-queue`
(тот же `INGEST_TOKEN`), какие тайтлы не читаются, дозагружает их
страницы напрямую (с сессией браузера) и присылает разобранные поля в
`POST /api/anime-recheck-result`.

Открытые вкладки узнают обо всём этом (и о новых карточках из
`/api/queue-ingest`) без перезагрузки — через `GET /api/live-updates`
(Server-Sent Events), который сайт открывает сам при загрузке.

Подписки, история серий и уведомления живут не в `localStorage`, а в
Postgres — `DATABASE_URL` обязателен, иначе (на локальном PGLite-фолбэке)
всё это обнулится при следующем рестарте сервера. Таблицы создавать
руками не нужно: сервер сам применяет `migrations/*.sql` к той базе, к
которой подключён, при первом обращении к ней после старта, и записывает
применённое в таблицу `_migrations`.

Именно сервер, а не сборка: `npm run build` тоже дёргает `npm run db:migrate`,
но деплой идёт по SSH, а такой шелл не наследует `Environment=` из
systemd-юнита — без `DATABASE_URL` мигратор молча пропускает файлы и выходит
с нулевым кодом. Раньше это оставляло сервис с `DATABASE_URL`, но без таблиц:
всё аниме-отслеживание падало на `relation "anime_tracking" does not exist`,
а колокольчик просто оставался пустым.

## Вишлист: аниме, которое ещё не вышло

Ссылка на **анонс** с AnimeGO карточку не создаёт: тайтл уходит в вишлист и
ждёт там выхода, помня выбранный приоритет. «Ещё не вышло» — это когда на
странице стоит «Анонс» **или** нет ни одной вышедшей серии (второе ловит
тайтлы, которым AnimeGO забыл переключить статус). Полностью вышедшее
(«Вышел») в вишлист не попадает никогда.

Открывается вишлист звездой в правом верхнем углу, слева от переключателя
темы (залита — панель открыта): справа выезжает панель со строками в формате
уведомлений, только вместо вышедших серий — дата выхода первой. Точной даты у
анонса часто нет, тогда показывается ровно то, что написано на сайте («весна
2026»). В строке — переключатель приоритета (менять можно в любой момент до
выхода), колокольчик и корзина; клик по постеру или названию открывает тайтл
на AnimeGO. Закрывается панель звездой или Esc — клик по странице её не роняет,
чтобы приоритеты можно было менять, листая очередь рядом.

Выходом считается то, что случится раньше: статус перестал быть «Анонс» **или**
в графике появилась первая вышедшая серия. Тогда сервер помечает строку, и сайт
заводит карточку в разделе «Аниме» — с тем самым приоритетом и сразу залитым
колокольчиком, так что дальше приходят уведомления о новых сериях.

**Одновременно двух уведомлений о первой серии не приходит.** У видов
уведомлений жёсткий порядок: «теперь онгоинг» побеждает «вышло полностью», а
то — «новую серию». Первая серия выходит тем же заходом чекера, каким тайтл
стартует, и отдельное «1 серия» о ней было бы дублем — карточка ведь только
что появилась именно потому, что аниме вышло. Сама карточка уведомлений не
порождает вовсе, а её подписка к этому моменту уже стоит на строке, так что
второму уведомлению взяться неоткуда.

Вишлист, в отличие от очереди, живёт не в `localStorage`, а в базе — той же
строкой `anime_tracking`, что и обычная карточка (поля `wishlist`,
`wishlist_priority`, `release_date`, `aired_at`). Поэтому он один на все
устройства, а перечитывается тем же чекером раз в 3 часа. Очередь-то у каждого
браузера своя, так что вышедший тайтл висит в списке ещё 30 дней: карточку
создаёт себе каждый браузер при ближайшем открытии сайта (повторно не создаёт —
помнит забранное в `localStorage`), а потом строка выпадает из вишлиста сама.

Кнопка Tampermonkey на странице AnimeGO работает так же: анонс уезжает в
вишлист, и скрипт пишет «Ещё не вышло — в вишлисте» вместо «Добавлено в
очередь».

## Tampermonkey-скрипт: `qwill-yt.user.js`

Скрипт добавляет на YouTube два способа закинуть видео в очередь, не заходя
на сайт:

1. **Кнопка на странице просмотра.** Слева от кнопки лайка появляется
   круглая кнопка со значком ссылки, оформленная в нейтральных тонах самого
   YouTube (подстраивается под светлую/тёмную тему). Клик — кнопка плавно
   растягивается в кластер из трёх сегментов с разделителями: огонёк
   (Важно), жёлтый кружок (Средне), серый кружок (Позже). Клик по сегменту —
   кнопка сужается обратно, на пару секунд показывается галочка об успехе
   (или предупреждающий значок, если запрос не прошёл).
2. **Контекстное меню в любой ленте.** Правый клик по карточке видео —
   на главной, в поиске, в блоке «Далее»/рекомендациях, в плейлистах —
   открывает вместо системного меню Chrome вертикальное меню с теми же
   тремя пунктами (Важно / Средне / Позже) для видео под курсором, включая
   момент, когда на карточке уже проигрывается автопревью.

Оба способа шлют ссылку на видео на `qwill-yt.mooo.com/api/queue-ingest`
через `GM_xmlhttpRequest` (в обход CORS), сайт подхватывает её при следующем
открытии.

На `animego.me` (и зеркалах `.org` / `.one` / `.club`) скрипт добавляет на
странице тайтла ещё одну иконку в правый угол шапки сайта, рядом с поиском,
колокольчиком и переключателем темы. Клик открывает выпадающее меню сайта
(`dropdown-menu`) с тремя приоритетами, под ними показывается статус
отправки. Вёрстку страницы это не двигает: кнопка живёт в шапке, меню
позиционируется абсолютно.

В отличие от вставки ссылки на сайте, здесь данные читаются из DOM уже
открытой страницы, поэтому работают и тайтлы, скрытые от неавторизованных.
Ответ сервера пишется в консоль строкой `[QWYT] ingest <статус> <тело>` —
по ней видно, почему отправка не прошла.

### Установка

1. Установите расширение [Tampermonkey](https://www.tampermonkey.net/).
2. Откройте `qwill-yt.user.js`, скопируйте содержимое в новый скрипт
   Tampermonkey (Dashboard → «+»).
3. Замените `__INGEST_TOKEN__` на значение `INGEST_TOKEN`, которое задано в
   systemd-юните сайта на сервере.
4. Сохраните — готово, ничего больше настраивать не нужно.

Скрипт уже установлен и обновился в репозитории? Тогда откройте его в
Tampermonkey, замените содержимое на новое и снова подставьте
`INGEST_TOKEN` вместо `__INGEST_TOKEN__` — токен в репозитории не хранится,
поэтому при каждом обновлении его нужно вписать заново.
